import { View } from "react-native";

import { Text } from "@components";

function Categories() {
  return (
    <View>
      <Text.Title value={"Categories"} />
    </View>
  );
}

export default Categories;
